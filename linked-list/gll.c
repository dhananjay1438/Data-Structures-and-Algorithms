#include <stdbool.h>
#include <stdio.h>
typedef struct GLLNode {
  struct GLLNode *down;
  struct GLLNode *next;
  int coeff;
  int exp;
} Node;
Node *head = NULL;
void insert(int data) {}
int main(void) {
  char str[] = "(a,(b,c), d)";
  return 0;
}
