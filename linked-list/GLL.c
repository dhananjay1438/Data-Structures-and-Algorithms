#include <stdio.h>

struct node {
  int data;
  struct node *next;
  struct node *down;
};
typedef struct node Node;
struct node *head = NULL;
struct node *tail = NULL;

void insert() {}
int main(void) { return 0; }
